Sources for the plugin `shared.android.support.v4.mediacompat`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.android.support.v4.mediacompat" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
